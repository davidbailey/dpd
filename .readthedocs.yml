version: 2
formats: []

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.8"
  apt_packages:
    - python3-dev
    - libproj-dev
    - proj-data
    - proj-bin
    - libgeos-dev
    - gdal-bin
    - libgdal-dev

python:
  install:
    - requirements: docs/requirements.txt
  system_packages: true
